/**
 * Retired in V13. The time chip and plan-ahead sheet are gone from Home: the
 * phone's clock decides, and each door starts from a sensible time (see
 * lib/when.ts). This file stays only so an older copy of it can't break the
 * build.
 */
export {};
