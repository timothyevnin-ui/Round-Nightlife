/**
 * Retired. The drawn map was replaced by the real one (components/RealMap.tsx)
 * in V7. This file stays only so an older copy of it can't break the build
 * when a neighborhood is added.
 */
export {};
