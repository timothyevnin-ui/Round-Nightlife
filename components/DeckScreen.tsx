/**
 * Retired in V5. The swipe deck became the quick ones (components/QuickOnes.tsx).
 * This file exists only so an upload replaces the old one, which no longer
 * compiles against the new question bank. Safe to delete.
 */
export {};
